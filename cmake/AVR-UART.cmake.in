cmake_minimum_required(VERSION 2.8.2)

project(avr-uart-download NONE)

include(ExternalProject)

if(CMAKE_BUILD_TYPE EQUAL "RELEASE")
	ExternalProject_Add(avr-uart
		EXCLUDE_FROM_ALL
		GIT_REPOSITORY		https://github.com/andygock/avr-uart.git
		GIT_TAG				master
		SOURCE_DIR			"${CMAKE_BINARY_DIR}/avr-uart-src"
		BINARY_DIR			""
		CONFIGURE_COMMAND	""
		BUILD_COMMAND		""
		INSTALL_COMMAND		""
		TEST_COMMAND		""
#		UPDATE_DISCONNECTED	TRUE
		GIT_PROGRESS		TRUE
	)
else()
	ExternalProject_Add(avr-uart
		GIT_REPOSITORY		https://github.com/andygock/avr-uart.git 
		GIT_TAG				master
		SOURCE_DIR			"${CMAKE_BINARY_DIR}/avr-uart-src"
		BINARY_DIR			""
		CONFIGURE_COMMAND	""
		BUILD_COMMAND		""
		INSTALL_COMMAND		""
		TEST_COMMAND		""
#		UPDATE_DISCONNECTED	TRUE
		GIT_PROGRESS		TRUE
	)
endif()